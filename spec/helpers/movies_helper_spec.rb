require 'rails_helper'
require 'movies_helper'

describe MoviesHelper do
  describe "#calculate_rating" do
    it "calculates correct overall rating for a particular movie" do
      # result = calculate_rating(movie)

      # expect(result).to eq(3)
    end

    it "returns correct movies of particular rating" do
      # result = movies_rated(num)

    end
  end
end
